@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model Domain.SYS_POST
<style type="text/css">
    .gray-bg {
        background-color: white;
    }
</style>
@using (Ajax.BeginForm("Save", null, new AjaxOptions()
{
    HttpMethod = "Post",
    OnBegin = "SubAjax.Loading",
    OnComplete = "SubAjax.Complate",
    OnFailure = "SubAjax.Failure",
    OnSuccess = "SubAjax.Success"
},
                                           new { @class = "form-horizontal dig-from", @role = "form" }))
{
    @Html.HiddenFor(p => p.ID)
    @Html.HiddenFor(p => p.FK_DEPARTID)
    @Html.HiddenFor(p => p.CREATEDATE)
    @Html.HiddenFor(p => p.CREATEUSER)
    <div class="wrapper wrapper-content animated fadeInUp">
        <div class="row">
            <div class="ibox-detail-title">
                <i class="fa fa-pencil-square-o"></i>添加/修改職位
            </div>
            <div class="ibox-content">
                <div class="form-group">
                    <label class="col-xs-2 control-label">職位名稱：</label>
                    <div class="col-xs-10">
                        @Html.TextBoxFor(p => p.POSTNAME, new { @class = "form-control", @placeholder = "請輸入職位名稱", @datatype = "*", @nullmsg = "請輸入職位名稱！", @errormsg = "請輸入職位名稱！" })
                    </div>
                </div>
                <div class="hr-line-dashed"></div>
                <div class="row">
                    <div class="col-xs-6">
                        <div class="form-group">
                            <label class="col-xs-4 control-label">職位類型：</label>
                            <div class="col-xs-8">
                                <select id="posttype" name="posttype" class="input-sm form-control inline">
                                    @{ var post = ViewData["postType"] as List<Domain.SYS_CODE>;
                                        foreach (var item in post)
                                        {
                                            <option value="@item.CODEVALUE" @(Model.POSTTYPE == item.CODEVALUE ? "selected" : "")>@item.NAMETEXT</option>
                                        }
                                    }
                                </select>

                            </div>
                        </div>
                    </div>
                    <div class="col-xs-6">
                        <div class="form-group">
                            <label class="col-xs-4 control-label">職位排序：</label>
                            <div class="col-xs-8">
                                @Html.TextBoxFor(p => p.SHOWORDER, new { @class = "form-control", @id = "modulepath", @placeholder = "請輸入職位排序值", @datatype = "n", @nullmsg = "請輸入職位排序值", @errormsg = "只能輸入數位" })
                            </div>
                        </div>
                    </div>
                </div>
                <div class="hr-line-dashed"></div>
                <div class="form-group">
                    <label class="col-xs-2 control-label">備註：</label>
                    <div class="col-xs-10">
                        @Html.TextAreaFor(p => p.REMARK, new { @class = "form-control", @row = "4", @placeholder = "備註" })
                    </div>
                </div>
                <div class="hr-line-dashed"></div>
                <div class="text-center">
                    <button class="btn btn-primary btn-save" type="submit"><i class="fa fa-check"></i> <span>確定保存</span></button>
                    <button class="btn btn-warning" id="btn-dig-close" type="button"><i class="fa fa-reply-all"></i> 取消返回</button>
                </div>
            </div>
        </div>
    </div>
                                        }
@section scripts{
    <script type="text/javascript">
        $(function () {
            $('.form-horizontal').initValidform();
        });
    </script>
}

